[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
repository = { path = "../repository" }
dotenvy = "0.15.7"
rocket = { version = "0.5.1", features = ["json"] }
serde = { version = "1.0.204", features = ["derive"] }
serde_json = "1.0.120"
